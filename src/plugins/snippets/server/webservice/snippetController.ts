import { Controller }    from '@nestjs/common';
import { PaginatedList } from '@vendure/core';

@Controller('snippets')
export class SnippetController {
  constructor() {}

  // @Get()
  // findAll(@Ctx() ctx: RequestContext): Promise<PaginatedList<Translated<Snippet>>> {
  //
  // }
}

